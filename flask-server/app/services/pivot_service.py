def calculate_pivot_bpm(bpm1: float, bpm2: float) -> float:
    print(f"Service is calculating pivot for: {bpm1} and {bpm2}")
    
    # --- pivot algorithm here ---
    # For now, it's just adding the two BPMs as a placeholder.
    combined_bpm = bpm1 + bpm2
    
    return combined_bpm